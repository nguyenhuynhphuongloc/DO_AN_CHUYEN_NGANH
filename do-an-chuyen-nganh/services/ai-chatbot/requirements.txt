# === CORE API ===
fastapi
uvicorn[standard]
pydantic

# === DATA / UTIL ===
python-dotenv
httpx
unidecode

# === AI / NLP (via Groq API - no local models needed) ===
# Removed: torch, transformers, accelerate, bitsandbytes
# Removed: sentence-transformers (embedding fallback still available if needed)
# Removed: underthesea

# === EVALUATION / PLOTTING ===
matplotlib
pandas
tabulate

# === EXPORT ===
python-docx
