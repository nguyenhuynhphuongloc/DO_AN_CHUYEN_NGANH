# Repository Agent Guidance

- When working with documents, prefer using the `doc` skill.
- Save all generated documents to `output/doc/`.
- Use a formal academic tone for thesis-related content.
- Use `tmp/docs/` for temporary document render artifacts and clean them up after validation.
